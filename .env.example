# WSIM Root Environment Configuration
# Copy this file to .env and customize as needed

# Security Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=dev-jwt-secret-change-in-production
SESSION_SECRET=dev-session-secret-change-in-production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef
COOKIE_SECRET=dev-cookie-secret-change-in-production

# BSIM Providers Configuration (JSON array)
# Each provider needs: bsimId, name, issuer, clientId, clientSecret
BSIM_PROVIDERS='[{"bsimId":"td-bank","name":"TD Canada Trust","issuer":"http://localhost:3002","clientId":"wsim-client","clientSecret":"wsim-secret"}]'
