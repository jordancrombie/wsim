# WSIM Backend Configuration

# Server
NODE_ENV=development
PORT=3003

# URLs
APP_URL=http://localhost:3003
FRONTEND_URL=http://localhost:3004
AUTH_SERVER_URL=http://localhost:3005

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/wsim

# Security (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=dev-jwt-secret-change-in-production
SESSION_SECRET=dev-session-secret-change-in-production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# BSIM Providers (JSON array)
# Each provider needs: bsimId, name, issuer, clientId, clientSecret
# For local standalone development:
BSIM_PROVIDERS='[{"bsimId":"td-bank","name":"TD Canada Trust","issuer":"http://localhost:3002","clientId":"wsim-wallet","clientSecret":"wsim-secret"}]'

# For BSIM-orchestrated dev environment (wsim-dev.banksim.ca):
# BSIM_PROVIDERS='[{"bsimId":"bsim","name":"Bank Simulator","issuer":"https://auth-dev.banksim.ca","clientId":"wsim-wallet","clientSecret":"your-client-secret"}]'

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3004,http://localhost:3005
