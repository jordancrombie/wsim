<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - WSIM Wallet</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }
    .checkout-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 25px 80px rgba(0, 0, 0, 0.3);
      max-width: 440px;
      width: 100%;
      overflow: hidden;
    }
    .checkout-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 1.5rem 2rem;
      text-align: center;
    }
    .checkout-header .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      margin-bottom: 0.25rem;
    }
    .checkout-header .logo-icon { font-size: 1.75rem; }
    .checkout-header .logo-text { font-size: 1.25rem; font-weight: 700; }
    .checkout-header .subtitle { font-size: 0.8125rem; opacity: 0.9; }
    .checkout-content { padding: 1.5rem 1.75rem 1.75rem; }
    .step-indicator { display: flex; justify-content: center; gap: 0.5rem; margin-bottom: 1.25rem; }
    .step-dot { width: 8px; height: 8px; border-radius: 50%; background: #e5e7eb; }
    .step-dot.active { background: #667eea; width: 24px; border-radius: 4px; }
    .step-dot.completed { background: #22c55e; }
    h1 { font-size: 1.125rem; font-weight: 600; color: #1f2937; margin-bottom: 0.25rem; text-align: center; }
    .page-subtitle { font-size: 0.8125rem; color: #6b7280; text-align: center; margin-bottom: 1.25rem; }
    .payment-summary {
      background: linear-gradient(135deg, #f8f9fc 0%, #f1f5f9 100%);
      border-radius: 12px;
      padding: 1rem 1.25rem;
      margin-bottom: 1.25rem;
      border: 1px solid #e2e8f0;
      text-align: center;
    }
    .payment-merchant { font-size: 0.8125rem; color: #64748b; margin-bottom: 0.125rem; }
    .payment-amount { font-size: 1.75rem; font-weight: 700; color: #1e293b; letter-spacing: -0.5px; }
    .payment-currency { font-size: 0.875rem; font-weight: 500; color: #64748b; }
    .card-list { display: flex; flex-direction: column; gap: 0.625rem; margin-bottom: 1.25rem; }
    .card-option {
      display: flex;
      align-items: center;
      gap: 0.875rem;
      padding: 0.875rem;
      border: 2px solid #e2e8f0;
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.15s ease;
      background: #fafbfc;
    }
    .card-option:hover { border-color: #667eea; background: #f8f7ff; }
    .card-option:has(input:checked) {
      border-color: #667eea;
      background: linear-gradient(135deg, #f8f7ff 0%, #f0eeff 100%);
      box-shadow: 0 2px 8px rgba(102, 126, 234, 0.15);
    }
    .card-option input[type="radio"] { display: none; }
    .card-icon {
      width: 44px;
      height: 28px;
      background: linear-gradient(135deg, #374151 0%, #1f2937 100%);
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.625rem;
      color: white;
      font-weight: 700;
      flex-shrink: 0;
      letter-spacing: 0.5px;
    }
    .card-icon.visa { background: linear-gradient(135deg, #1a1f71 0%, #0d47a1 100%); }
    .card-icon.mastercard { background: linear-gradient(135deg, #eb001b 0%, #f79e1b 100%); }
    .card-icon.amex { background: linear-gradient(135deg, #006fcf 0%, #0077d4 100%); }
    .card-info { flex: 1; min-width: 0; }
    .card-type { font-weight: 600; color: #1e293b; font-size: 0.875rem; }
    .card-number {
      font-family: 'SF Mono', Monaco, 'Courier New', monospace;
      color: #64748b;
      font-size: 0.8125rem;
      letter-spacing: 0.25px;
    }
    .card-bank { font-size: 0.6875rem; color: #94a3b8; margin-top: 0.125rem; }
    .card-check {
      width: 22px;
      height: 22px;
      border: 2px solid #cbd5e1;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.75rem;
      color: transparent;
      flex-shrink: 0;
      transition: all 0.15s ease;
    }
    .card-option:has(input:checked) .card-check {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-color: transparent;
      color: white;
    }
    .button-group { display: flex; gap: 0.75rem; }
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      flex: 1;
      padding: 0.8125rem 1rem;
      border: none;
      border-radius: 10px;
      font-size: 0.9375rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.15s ease;
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }
    .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 4px 12px rgba(102, 126, 234, 0.35); }
    .btn-secondary { background: #f1f5f9; color: #475569; }
    .btn-secondary:hover { background: #e2e8f0; }
    .empty-state { text-align: center; padding: 2rem 1rem; }
    .empty-state .empty-icon { font-size: 2.5rem; margin-bottom: 0.75rem; opacity: 0.4; }
    .empty-state p { color: #64748b; font-size: 0.875rem; }
    .checkout-footer {
      text-align: center;
      padding: 0.875rem 1.5rem;
      background: #f8fafc;
      border-top: 1px solid #f1f5f9;
    }
    .checkout-footer .secure-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
      font-size: 0.75rem;
      color: #64748b;
    }
  </style>
</head>
<body>
  <div class="checkout-container">
    <div class="checkout-header">
      <div class="logo">
        <span class="logo-icon">&#128179;</span>
        <span class="logo-text">WSIM Wallet</span>
      </div>
      <div class="subtitle">Secure Checkout</div>
    </div>
    <div class="checkout-content">
      <div class="step-indicator">
        <div class="step-dot completed"></div>
        <div class="step-dot completed"></div>
        <div class="step-dot active"></div>
      </div>

      <h1><%= title %></h1>
      <p class="page-subtitle">Choose a card to complete your payment</p>

      <% if (typeof payment !== 'undefined' && (payment.amount || payment.merchantName)) { %>
        <div class="payment-summary">
          <% if (payment.merchantName) { %>
            <div class="payment-merchant">Payment to <%= payment.merchantName %></div>
          <% } else if (clientName) { %>
            <div class="payment-merchant">Payment to <%= clientName %></div>
          <% } %>
          <% if (payment.amount) { %>
            <div class="payment-amount">
              $<%= typeof payment.amount === 'number' ? payment.amount.toFixed(2) : payment.amount %>
              <span class="payment-currency"><%= payment.currency || 'CAD' %></span>
            </div>
          <% } %>
        </div>
      <% } %>

      <form method="POST" action="/interaction/<%= uid %>/select-card">
        <% if (cards && cards.length > 0) { %>
          <div class="card-list">
            <% cards.forEach(function(card, index) { %>
              <%
                const cardTypeLower = (card.cardType || '').toLowerCase();
                let iconClass = '';
                let iconText = '';
                if (cardTypeLower.includes('visa')) { iconClass = 'visa'; iconText = 'VISA'; }
                else if (cardTypeLower.includes('master')) { iconClass = 'mastercard'; iconText = 'MC'; }
                else if (cardTypeLower.includes('amex') || cardTypeLower.includes('american')) { iconClass = 'amex'; iconText = 'AMEX'; }
                else { iconText = '&#128179;'; }
              %>
              <label class="card-option">
                <input type="radio" name="walletCardId" value="<%= card.id %>" <%= index === 0 ? 'checked' : '' %> required>
                <div class="card-icon <%= iconClass %>"><%= iconText %></div>
                <div class="card-info">
                  <div class="card-type"><%= card.cardType %></div>
                  <div class="card-number">**** **** **** <%= card.lastFour %></div>
                  <div class="card-bank">via <%= card.enrollment.bsimId %></div>
                </div>
                <div class="card-check">&#10003;</div>
              </label>
            <% }); %>
          </div>

          <div class="button-group">
            <button type="submit" formaction="/interaction/<%= uid %>/abort" class="btn btn-secondary">
              Cancel
            </button>
            <button type="submit" class="btn btn-primary">
              &#128274; Pay Now
            </button>
          </div>
        <% } else { %>
          <div class="empty-state">
            <div class="empty-icon">&#128179;</div>
            <p>No cards available in your wallet</p>
            <p style="font-size: 0.75rem; margin-top: 0.5rem; color: #94a3b8;">
              Please enroll a bank account first.
            </p>
          </div>
          <button type="submit" formaction="/interaction/<%= uid %>/abort" class="btn btn-secondary" style="width: 100%;">
            Return to Merchant
          </button>
        <% } %>
      </form>
    </div>
    <div class="checkout-footer">
      <span class="secure-badge">&#128274; Secured by WSIM</span>
    </div>
  </div>
</body>
</html>
