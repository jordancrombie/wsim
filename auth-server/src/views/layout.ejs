<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - WSIM Wallet</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .checkout-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 25px 80px rgba(0, 0, 0, 0.3);
      max-width: 440px;
      width: 100%;
      overflow: hidden;
    }

    .checkout-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 1.5rem 2rem;
      text-align: center;
    }

    .checkout-header .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      margin-bottom: 0.5rem;
    }

    .checkout-header .logo-icon {
      font-size: 2rem;
    }

    .checkout-header .logo-text {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -0.5px;
    }

    .checkout-header .subtitle {
      font-size: 0.875rem;
      opacity: 0.9;
    }

    .checkout-content {
      padding: 1.5rem 2rem 2rem;
    }

    .step-indicator {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
    }

    .step-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #e5e7eb;
    }

    .step-dot.active {
      background: #667eea;
      width: 24px;
      border-radius: 4px;
    }

    .step-dot.completed {
      background: #22c55e;
    }

    h1 {
      font-size: 1.25rem;
      font-weight: 600;
      color: #1f2937;
      margin-bottom: 0.5rem;
      text-align: center;
    }

    .page-subtitle {
      font-size: 0.875rem;
      color: #6b7280;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }

    label {
      display: block;
      font-size: 0.875rem;
      font-weight: 600;
      color: #374151;
      margin-bottom: 0.5rem;
    }

    input[type="email"],
    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 0.875rem 1rem;
      border: 2px solid #e5e7eb;
      border-radius: 12px;
      font-size: 1rem;
      transition: all 0.2s;
      background: #f9fafb;
    }

    input:focus {
      outline: none;
      border-color: #667eea;
      background: white;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    input::placeholder {
      color: #9ca3af;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.875rem 1.5rem;
      border: none;
      border-radius: 12px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      text-align: center;
    }

    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      width: 100%;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
    }

    .btn-primary:active {
      transform: translateY(0);
    }

    .btn-secondary {
      background: #f3f4f6;
      color: #4b5563;
    }

    .btn-secondary:hover {
      background: #e5e7eb;
    }

    .btn-icon {
      font-size: 1.1rem;
    }

    .error {
      background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
      color: #991b1b;
      padding: 0.875rem 1rem;
      border-radius: 12px;
      margin-bottom: 1.25rem;
      font-size: 0.875rem;
      border: 1px solid #fecaca;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .error::before {
      content: "!";
      display: flex;
      align-items: center;
      justify-content: center;
      width: 20px;
      height: 20px;
      background: #dc2626;
      color: white;
      border-radius: 50%;
      font-size: 0.75rem;
      font-weight: 700;
      flex-shrink: 0;
    }

    /* Card Selection Styles */
    .card-list {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
    }

    .card-option {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem;
      border: 2px solid #e5e7eb;
      border-radius: 14px;
      cursor: pointer;
      transition: all 0.2s;
      background: #fafafa;
    }

    .card-option:hover {
      border-color: #667eea;
      background: #f8f7ff;
      transform: translateY(-1px);
    }

    .card-option.selected,
    .card-option:has(input:checked) {
      border-color: #667eea;
      background: linear-gradient(135deg, #f8f7ff 0%, #f0eeff 100%);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.15);
    }

    .card-option input[type="radio"] {
      display: none;
    }

    .card-icon {
      width: 48px;
      height: 32px;
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.75rem;
      color: white;
      font-weight: 600;
      flex-shrink: 0;
    }

    .card-icon.visa { background: linear-gradient(135deg, #1a1f71 0%, #0d47a1 100%); }
    .card-icon.mastercard { background: linear-gradient(135deg, #eb001b 0%, #f79e1b 100%); }
    .card-icon.amex { background: linear-gradient(135deg, #006fcf 0%, #0077d4 100%); }

    .card-info {
      flex: 1;
      min-width: 0;
    }

    .card-type {
      font-weight: 600;
      color: #1f2937;
      font-size: 0.9375rem;
    }

    .card-number {
      font-family: 'SF Mono', 'Monaco', 'Inconsolata', 'Fira Code', monospace;
      color: #6b7280;
      font-size: 0.875rem;
      letter-spacing: 0.5px;
    }

    .card-bank {
      font-size: 0.75rem;
      color: #9ca3af;
      margin-top: 0.125rem;
    }

    .card-check {
      width: 24px;
      height: 24px;
      border: 2px solid #d1d5db;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s;
      flex-shrink: 0;
      color: transparent;
      font-size: 0.75rem;
    }

    .card-option.selected .card-check,
    .card-option:has(input:checked) .card-check {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-color: transparent;
      color: white;
    }

    /* Payment Info */
    .payment-summary {
      background: linear-gradient(135deg, #f8f9fc 0%, #f3f4f6 100%);
      border-radius: 14px;
      padding: 1.25rem;
      margin-bottom: 1.5rem;
      border: 1px solid #e5e7eb;
      text-align: center;
    }

    .payment-merchant {
      font-size: 0.875rem;
      color: #6b7280;
      margin-bottom: 0.25rem;
    }

    .payment-amount {
      font-size: 2rem;
      font-weight: 700;
      color: #1f2937;
      letter-spacing: -1px;
    }

    .payment-currency {
      font-size: 1rem;
      font-weight: 500;
      color: #6b7280;
      margin-left: 0.25rem;
    }

    /* Legacy payment-info support */
    .payment-info {
      background: linear-gradient(135deg, #f8f9fc 0%, #f3f4f6 100%);
      border-radius: 14px;
      padding: 1.25rem;
      margin-bottom: 1.5rem;
      border: 1px solid #e5e7eb;
      text-align: center;
    }

    .payment-info p {
      font-size: 0.875rem;
      color: #6b7280;
      margin-bottom: 0.25rem;
    }

    .payment-info .payment-amount {
      font-size: 2rem;
      font-weight: 700;
      color: #1f2937;
      letter-spacing: -1px;
      margin-top: 0.25rem;
    }

    /* Button Groups */
    .button-group {
      display: flex;
      gap: 0.75rem;
    }

    .button-group .btn {
      flex: 1;
    }

    /* Scopes/Permissions List */
    .scopes-list {
      margin-bottom: 1.5rem;
    }

    .scope-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 0;
      border-bottom: 1px solid #f3f4f6;
    }

    .scope-item:last-child {
      border-bottom: none;
    }

    .scope-icon {
      width: 28px;
      height: 28px;
      background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #059669;
      font-size: 0.875rem;
      flex-shrink: 0;
    }

    .scope-name {
      font-size: 0.9375rem;
      color: #374151;
    }

    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 2rem 1rem;
    }

    .empty-state .empty-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      opacity: 0.5;
    }

    .empty-state p {
      color: #6b7280;
      font-size: 0.9375rem;
    }

    /* Footer */
    .checkout-footer {
      text-align: center;
      padding: 1rem 2rem;
      background: #f9fafb;
      border-top: 1px solid #f3f4f6;
    }

    .checkout-footer p {
      font-size: 0.75rem;
      color: #9ca3af;
    }

    .checkout-footer .secure-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
      color: #6b7280;
    }

    /* Cancel link */
    .cancel-link {
      display: block;
      text-align: center;
      margin-top: 1rem;
      font-size: 0.875rem;
      color: #6b7280;
      text-decoration: none;
    }

    .cancel-link:hover {
      color: #4b5563;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="checkout-container">
    <div class="checkout-header">
      <div class="logo">
        <span class="logo-icon">&#128179;</span>
        <span class="logo-text">WSIM Wallet</span>
      </div>
      <div class="subtitle">Secure Wallet Checkout</div>
    </div>
    <div class="checkout-content">
      <%- body %>
    </div>
    <div class="checkout-footer">
      <p class="secure-badge">
        &#128274; Secured by WSIM
      </p>
    </div>
  </div>
</body>
</html>
